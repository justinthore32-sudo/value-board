# Ne pas remplir ce fichier à la main : lance `python scripts/set_credentials.py`
# à la racine du projet, il génère .streamlit/secrets.toml (jamais commité)
# avec les vraies valeurs.
#
# Pour un déploiement sur Streamlit Community Cloud, recopie le contenu de
# ton .streamlit/secrets.toml local dans les "Secrets" de l'app (menu de
# l'app → Settings → Secrets).

AUTH_USERNAME = "ton_nom_utilisateur"
AUTH_PASSWORD_HASH = "hash_pbkdf2_hex_genere_par_le_script"
AUTH_SALT = "sel_hex_genere_par_le_script"
